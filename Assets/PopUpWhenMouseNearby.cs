using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class PopUpWhenMouseNearby : MonoBehaviour
{
    //private Vector3 initialPos;
    //public float slideSpeed;
    ////public float slideTime;
    //public float closeEnoughDistance;
    //public GameObject RefPoint;
    //public bool mouseOver = false;
    //// Start is called before the first frame update
    //void Start()
    //{
    //    initialPos = transform.position;

    //}

    //private void Update()
    //{
    //    if (mouseOver)
    //    {

    //        if (Vector3.Distance(this.transform.position, initialPos) > closeEnoughDistance)
    //        {

    //        }
    //        this.transform.position = Vector3.Lerp(this.transform.position, initialPos, slideSpeed * Time.deltaTime);

    //    }
    //    if (!mouseOver)
    //    {
    //        if (Vector3.Distance(this.transform.position, RefPoint.transform.position) > closeEnoughDistance)
    //        {
    //            this.transform.position = Vector3.Lerp(this.transform.position, RefPoint.transform.position, slideSpeed * Time.deltaTime);
    //        }

    //    }

    //}

    



}
