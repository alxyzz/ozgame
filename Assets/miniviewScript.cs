using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class miniviewScript : MonoBehaviour
{
    // Start is called before the first frame update
    public MiniviewClickDetector RectClickDetector;
    [HideInInspector]
    public EntityDefiner.Character associatedCharacter; //this is defined on start in RefreshViewPlayer(), UserInterfaceHelper






}
